import type { ThemeProviderProps } from "../theme-provider";

/**
 * @description will change to interface if more combination of provider happens in the future
 */
export type GlobalProviderProps = ThemeProviderProps;
